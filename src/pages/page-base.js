export class PageBase {
    constructor(headContent, mainContent, footContent){
        this.headContent = headContent;
        this.mainContent = mainContent;
        this.footContent = footContent;
    }

    renderPage() {

    }
}